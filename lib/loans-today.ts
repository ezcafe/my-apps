/** Local calendar date (YYYY-MM-DD) for loan due/overdue comparisons. */
export function getLoansTodayIso(): string {
  const d = new Date();
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
}
