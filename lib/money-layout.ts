/** Full-width cell in the Money shell grid (2 / 6 / 12 columns). */
export const MONEY_FULL_SPAN =
  "col-span-2 min-w-0 md:col-span-6 lg:col-span-12";

/** Vertical stack for dashboard pages: filters → metrics → chart → table. */
export const MONEY_DASHBOARD_STACK = "flex w-full min-w-0 flex-col gap-8";
