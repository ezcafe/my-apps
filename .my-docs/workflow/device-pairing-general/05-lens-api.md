# API lens: device-pairing-general

**Result:** clean  
Mint `{ apps, scopes?, workspaceId? }` → 201 code; redeem `{ code }` → token with stored grants. Errors use pairErrorResponse. No Critical/Major.
