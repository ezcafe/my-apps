# Security lens: device-pairing-general

**Result:** clean  
Session + same-origin mint; public rate-limited redeem; code hashed; apps chosen only at mint with access assert; Bearer secret shown once in modal.
